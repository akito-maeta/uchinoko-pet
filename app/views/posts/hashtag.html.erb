<h1><%= @hashtag.hashname %>  の投稿一覧</h1>
<% @post.each do |post| %>
  <%= link_to post_path(@post.ids) do %>
    <%= attachment_image_tag post, :image, :fill, 100, 100, format: 'jpeg' %>
  <% end %>
<% end %>